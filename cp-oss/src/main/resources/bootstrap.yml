spring:
  application:
    name: cp-oss-dev
#  profiles:
#    active: dev
  cloud:
    nacos:
      config:
        server-addr: http://192.168.8.128:8848
        prefix: ${spring.application.name}
        group: DEFAULT_GROUP
        namespace:
        file-extension: yaml
      discovery:
        enabled: true
        server-addr: http://192.168.8.128:8848


